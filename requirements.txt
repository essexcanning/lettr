# Core agent stack — voice via LiveKit. STT chain: ElevenLabs → Fireworks → Deepgram (fallback)
livekit-agents>=0.12.0
livekit-plugins-openai>=0.10.0
livekit-plugins-elevenlabs>=0.7.0
livekit-plugins-deepgram>=0.6.0
livekit-plugins-silero>=0.7.0

# LangChain + LangGraph (orchestration + persistent state)
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-mongodb>=0.2.0
langgraph>=0.2.0
langgraph-checkpoint-mongodb>=0.1.0
langsmith>=0.1.0

# MongoDB + embeddings
pymongo[srv]>=4.6.0
voyageai>=0.3.0

# Web (dashboard + audio serving + LiveKit guest tokens)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
livekit-api>=0.8.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0
